import { Routes } from '@angular/router';
import {PipeLineComponent} from './pipe-line/pipe-line.component';

export const routes: Routes = [
    {path:"pipeline-designer", component: PipeLineComponent},
    
];
